# Fundamentos de JavaScript

## 1. Intruodução ao JavaScript

- O que é JavaScript e para que serve
- Onde o JavaScript é utilizado (navegadores, servidores, etc.)
- Como adicionar JavaScript a uma página HTML (`<script>` tag)
- Ferramentas essenciais: navegador (dev tools), editor de código

## 2. Fundamentos da Linguagem

- Tipos de dados (string, number, boolean, null, undefined, object, array, function, symbol, bigint, etc.)
- Declaração de variáveis (`var`, `let`, `const`)
- Operadores (aritméticos, de comparação, lógicos, etc.)
- Coerção de tipos e conversão de dados (implicita e explicita)

## 3. Controle de Fluxo

- Estruturas de controle (if, else, switch, else if)
- Operador ternário
- Loops (for, while, do...while)
- Controle de fluxo avançado (break, continue)
- Manipulação de erros (try...catch)

## 4. Estrutura de repetição

- `for` loop
- `while` loop
- `do...while` loop
- `for...in` loop
- `for...of` loop

## 5. Funções

- Declaração de funções (function declaration, function expression, arrow functions)
- Parâmetros e argumentos
- Expressões de função
- Arrow functions e suas peculiaridades (this, arguments, etc.)
- Escopo de variáveis (global, local, bloco)
- Retorno de valores
- Hoisting de funções e variáveis
- Funções anônimas e IIFE (Immediately Invoked Function Expressions)
- Funções de callback e funções de ordem superior

## 6. Arrays e Objetos

- Criar e manipular arrays (métodos como push, pop, shift, unshift, map, filter, reduce, etc.)
- Metodos de array avançados (`push`, `pop`, `shift`, `unshift`, `map`, `filter`, `reduce`, etc.)
- Criar e manipular objetos (propriedades, métodos, `this`, etc.)
- Acessar e modificar propriedades de objetos
- Iterar sobre objetos e arrays (for...in, for...of, etc.)

## 7. Conceitos importantes

- Valor vs. referência
- Truthy e falsy
- Desestruturação de arrays e objetos
- Spread e rest operators

## 8. Manipulação do DOM

- O que é o DOM (Document Object Model)
- Selecionar elementos (`querySelector`, `getElementById`, etc.)
- Manipular conteudo e atributos (`textContent`, `innerHTML`, `setAttribute`, etc.)
- Trabalhar com eventos (`addEventListener`, `removeEventListener`, etc.)
- Criar, adicionar e remover elementos (`createElement`, `appendChild`, `removeChild`, etc.)

## 9. Fetch e Promises

- O que são fetch e promises
- Realizar requisicoes HTTP (`fetch`)
- Encadeamento com `then()` , `catch()` e `finally()`
- `async` e `await`

## 10. Trabalhando com APIs e JSON

- O que é uma API
- O que é JSON
- Como trabalhar com APIs e JSON
- Exemplos de APIs e JSON
- Trabalhando com APIs externas
- Trabalhando com APIs internas
- Trabalhando com APIs de terceiros

## 11. Conclusão
